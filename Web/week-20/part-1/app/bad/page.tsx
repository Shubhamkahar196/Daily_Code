// "use client"

import { Button } from "@/components/count"  //this is server components and button is componenrts is client components

// import { useState } from "react";
export default function d(){
    // const [count, setCount] = useState(0);
    return <div>
        bad page
        <br />

        <Button/>

        {/* <button className="text-2xl border-4 bg-green-700" onClick={()=>{
            setCount(c => c+ 1)
        }}>
            Click me! ({count})
        </button> */}
    </div>
}